import React from "react";

type SubmitProps = {
  // props here
};

const Submit = (props: SubmitProps) => {
  return (
    <div>
      <h1>Submit page</h1>
    </div>
  );
};

export default Submit;

import React from "react";

type AboutPageProps = {
  // props here
};

const AboutPage = (props: AboutPageProps) => {
  return (
    <div>
      <h1>About page</h1>
    </div>
  );
};

export default AboutPage;
